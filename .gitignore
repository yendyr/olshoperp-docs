node_modules/
playwright-report/
test-results/
# Console capture dari Tee-Object saat agent run Playwright (bukan artifact resmi)
test-results-*.log
playwright/.cache/
tests/.auth/
.playwright-mcp/
.env
.env.local

# Catatan pribadi QA — hanya lokal, JANGAN commit/push
_local/

.local-node/
tests/scratch/

# Probe / IIFE sekali pakai Antigravity — DILARANG jadi jalur uji card.
# Pakai tests/specs/ (@TC-/@FLOW-/@ETM-) atau tests/scratch/ (gitignored).
tests/scripts/

# macOS
.DS_Store
