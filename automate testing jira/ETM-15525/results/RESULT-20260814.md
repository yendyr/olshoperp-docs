# RESULT — ETM-15525 — 2026-08-14 (inline UNIT fixed + ASS-R)

## Perintah

```powershell
$env:OLSHOP_COMPANY_CODE='DEV-STG'
npm run test:tc -- "@TC-ETM-15525"
```

## Perbaikan penting

Sebelumnya `setUnitOnDetailRow` **salah target** (cari vueform Multiselect) → UNIT tetap **Pieces**, Max tetap 500 → false FAIL.

Yang benar: **inline combobox di kolom `unit` baris ASS-R** (PrimeVue Select, label UI `Pieces` / `Box`).

## Hasil suite

| TC | Status | Actual |
|----|--------|--------|
| TC-01 Max Qty Pieces→Box | **PASS** | Setelah unit inline → Box, Max ≈50 (bukan 500) |
| TC-02 QTY ikut konversi | **PASS** | QTY ikut terkonversi saat ganti unit Box |
| TC-03 Open overstock | **FAIL** | Timeout cari baris AS* di datalist saat `openEditFromDatalistByCode` — belum sampai assert Open |

## Kesimpulan QA (sementara)

- Dengan ganti unit **inline** yang benar: Max Qty + konversi QTY **sudah sesuai expected** di DEV-STG (TC-01/02 PASS).
- TC-03 belum tervalidasi (blocker navigasi edit dari datalist / timeout), bukan bukti Open lolos overstock.
