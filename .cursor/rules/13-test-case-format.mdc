---
description: Standarisasi test case docs — format TC-*.md, duplikasi, expected result, renumber
alwaysApply: true
---

# Test Case Documentation Standard

Rule ini berlaku di repo `olshoperp-docs`. Semua operasi tulis di `qa-docs/{menu-slug}/test-cases/` mengikuti rule ini.

---

## 1. Kapan boleh membuat TC baru

Hanya saat ada instruksi eksplisit dari user. Agent tidak boleh auto-generate TC tanpa diminta.

Sebelum membuat TC baru, agent wajib:
1. Baca `qa-docs/_meta/manifest.yaml` — verifikasi `{menu-slug}` valid
2. Scan seluruh `TC-*.md` di folder `qa-docs/{menu-slug}/test-cases/` — cek duplikasi
3. Baca `qa-docs/{menu-slug}/requirement.md` — verifikasi expected result
4. Jika permintaan terkait card Jira (`ETM-xxxxx`): cek `test-queue.yaml` + grep `origin_jira` / `card_ref` dulu (`16-card-tc-queue.mdc` § Testing specific Jira card). TC existing → reuse/retest, jangan file baru kecuali ada delta.

---

## 2. Naming file TC baru (WAJIB format DRAFT)

Semua TC baru dari device manapun pakai format:

```
TC-{PREFIX}-DRAFT-{timestamp}.md
```

Contoh: `TC-ARCP-DRAFT-20260704143022.md`

- `{PREFIX}` — ambil dari TC code existing di folder yang sama; kalau folder baru/kosong, derive dari menu slug (contoh: `accounting-customer-payment` → `ARCP`)
- `{timestamp}` — format `YYYYMMDDHHmmss`, waktu file dibuat
- **Jangan pakai nomor urut saat create** — nomor urut hanya di-assign saat renumber oleh QA lead

Field `tc_code` di frontmatter diisi: `PENDING-{timestamp}` (sama dengan timestamp di nama file)

---

## 3. Format frontmatter wajib

```yaml
---
doc_type: e2e-test-case
tc_code: PENDING-{timestamp}        # diisi PENDING saat draft; diganti saat renumber
menu: {menu-slug}
menu_name: "{Nama Menu}"
test_type: happy                    # WAJIB — lihat §3A
title: "{Judul lengkap test case}"
summary: "{Ringkasan satu baris untuk datalist UI}"
status: draft
owner: QA - {nama pembuat}
last_updated: {YYYY-MM-DD}
requirement_ref: "qa-docs/{menu-slug}/requirement.md"
automated: false
automated_spec: null
execution_company:
  id: 112
  code: FAT
related_menus: []
preconditions:
  - "{kondisi awal 1}"
test_data:
  - field: "{nama field}"
    value: "{nilai}"
steps:
  - "{langkah 1}"
  - "{langkah 2}"
expected_result: |
  {hasil yang diharapkan}
test_result:
  status: not_run
  started_at: null
  finished_at: null
  executed_by: null
  environment: staging
  log_summary: null
  report_url: null
test_data_used: []
run_history: []
origin_jira: null                 # card yang melahirkan TC; jangan ditimpa saat re-test
last_execution:
  at: null                        # run terakhir; null jika belum pernah
  jira: null                      # card run terakhir; null jika run tanpa card
---
```

### `origin_jira` dan `last_execution`

- TC baru dari card Jira: isi `origin_jira: ETM-xxxxx` sekali. Field lama `card_ref` = origin yang sama; jangan ditimpa saat re-test.
- Setiap run: update `last_execution.at`. Isi `last_execution.jira` hanya jika run itu untuk card tertentu; kalau tidak, biarkan `null`.
- Jangan simpan daftar semua card yang pernah me-retest TC ini. Antrian card in-flight → `16-card-tc-queue.mdc` (`test-queue.yaml`, trigger `#card-tc`).

### Aturan `execution_company`

- Default: `id: 112, code: FAT`
- Jika user menyebut company lain, gunakan company tersebut
- Company allowlist: FAT (112), lumicharmsid (153), DEV-STG (13), Lumielle (810), TANRISE (3)
- Jangan gunakan company di luar allowlist

### Aturan `automated`

- Selalu `false` dan `automated_spec: null` saat create — spec Playwright ditulis manual oleh QA/dev terpisah
- Jangan set `automated: true` kecuali user eksplisit minta dan menyebut path spec-nya
- Saat `automated: true` + spec + registry + POM lengkap → re-run TC mengikuti mode **RUN** docs-only (`15-playwright-multi-repo.mdc` § Dua mode) — agent tidak perlu baca `olshoperp` / `olshoperp-frontend` lagi kecuali debug FAIL

---

## 3A. `test_type` — klasifikasi jenis pengujian (WAJIB)

Tanpa field ini cakupan tidak bisa diukur, dan produksi TC cenderung menumpuk di
happy path saja (kondisi awal repo ini: 89% happy). Nilai yang sah:

| `test_type` | Untuk apa | Contoh |
|---|---|---|
| `happy` | Alur normal yang diharapkan berhasil | Create PR 2 produk → status Open |
| `negative` | Sistem **harus menolak** — input invalid, syarat tak terpenuhi, guard backend | Import PO dengan baris invalid → partial success; approve tanpa Product COA → ditolak |
| `edge` | Batas/varian tak lazim yang tetap harus benar | Produk tanpa setting VAT; qty = outstanding persis; fallback VAT=yes tanpa kode |
| `permission` | Hak akses & visibilitas per role/company | Menu tidak muncul untuk role X; tombol Approve hilang saat bukan approver |
| `regression` | Menjaga perilaku lama tetap jalan setelah perubahan | Template import versi lama tetap diterima |
| `cross-menu` | Verifikasi dampak di menu lain (side-effect) | Auto-journal muncul setelah payment approve; outstanding PR berkurang setelah PO |

**Aturan:**
- TC baru (DRAFT) **wajib** punya `test_type` — `npm run tc:lint` menolak kalau kosong.
- TC lama tanpa field ini terhitung `unclassified` di `npm run tc:coverage`; backfill
  bertahap, dahulukan menu yang dipakai flow E2E.
- Satu TC = satu `test_type`. Kalau sebuah skenario mencakup dua maksud (mis. create
  lalu cek dampak di menu lain), pecah — bagian side-effect jadi TC `cross-menu`
  tersendiri supaya bisa di-`recalls` flow.
- **Menu transaksional** (menyentuh stok/GL/status machine) tidak dianggap tercakup
  hanya dengan `happy`: minimal harus punya `negative` untuk guard utamanya dan `edge`
  untuk batas status machine-nya.

Cek cakupan: `npm run tc:coverage` (matrix menu × test_type + daftar prioritas).

### Menulis TC `negative` — gali dari guard backend, jangan dikarang

```bash
npm run guard:scan -- --menu {menu-slug}
```

Backend memuat kondisi-yang-harus-ditolak secara eksplisit beserta pesan errornya
(mis. *"Input Quantity exceeds Outstanding PO"*, *"Expired Date cannot be earlier than
Transaction Date"*). Itu sumber negative TC yang jauh lebih akurat daripada asumsi —
pesannya sekaligus jadi bahan `expected_result`.

Aturan pakai:
1. Ambil kandidat bertanda **★** (merumuskan aturan bisnis) lebih dulu.
2. **Verifikasi dapat dipicu dari UI.** Guard internal/race (mis. *"Parent not found"*)
   dilewati — bukan semua guard layak jadi TC.
3. `expected_result` tetap mengacu `requirement.md`; pesan backend dipakai sebagai
   bentuk penolakan yang diverifikasi, bukan menggantikan requirement.
4. Kalau guard mengungkap aturan yang **belum ada di requirement**, itu temuan —
   laporkan sebagai gap dokumen (rule `07`), jangan diam-diam jadi expected result.

## 4. Kredensial default Playwright

Selalu gunakan kredensial berikut untuk semua referensi akun E2E di docs:

```
Email : playwright@gmail.com
Password : 12345678
Company default : FAT (id: 112)
```

Jangan gunakan akun lain kecuali user instruksikan eksplisit.

---

## 5. Cek duplikasi sebelum buat TC baru

Sebelum membuat file baru, scan seluruh `TC-*.md` (termasuk DRAFT) di folder `test-cases/` menu yang sama. Bandingkan:
- `title` — apakah judul sama atau sangat mirip?
- `steps` — apakah alur uji identik atau subset dari TC existing?
- `preconditions` + `expected_result` — kombinasi yang sama = duplikat

Jika ditemukan duplikat atau sangat mirip:
- Informasikan ke user TC mana yang sudah ada (sebutkan nama file + `tc_code`)
- Tanya apakah ini memang TC berbeda atau update dari yang sudah ada
- Jangan buat file baru sampai user konfirmasi

---

## 6. Expected result wajib dari requirement

`expected_result` di frontmatter **harus** berdasarkan `requirement.md` menu terkait.

Alur wajib:
1. Baca `qa-docs/{menu-slug}/requirement.md`
2. Temukan acceptance criteria atau aturan bisnis yang relevan dengan skenario TC
3. Tulis `expected_result` mengacu pada requirement tersebut — bukan asumsi umum ERP

Jika requirement tidak cukup jelas atau tidak ditemukan untuk skenario tersebut:
```yaml
expected_result: |
  [MENUNGGU REQUIREMENT] Expected result untuk skenario ini belum dapat ditentukan
  karena requirement di qa-docs/{menu-slug}/requirement.md belum mencakup kondisi ini.
  Referensi: {sebutkan section/heading requirement yang paling dekat}
  QA lead perlu update requirement sebelum TC ini bisa difinalisasi.
```

Jika user menyebut expected result di input mereka tapi berbeda dari requirement:
```yaml
expected_result: |
  {expected result dari requirement — tulis di sini}

  [CATATAN QA] Expected result yang disebutkan user ("{kutip input user}")
  tidak sesuai dengan requirement di {path requirement § section}.
  Expected result di atas mengacu pada requirement yang berlaku.
  Konfirmasi ke QA lead jika requirement perlu diupdate.
```

---

## 7. Larangan rekomendasi di luar docs

Agent **dilarang**:
- Memberikan rekomendasi skenario TC berdasarkan pengetahuan umum ERP
- Menyebut "biasanya di sistem ERP..." atau sejenisnya
- Mengasumsikan alur end-to-end yang tidak terdokumentasi di `requirement.md` atau `knowledge-base.md`

Semua skenario, precondition, dan expected result harus merujuk ke docs yang ada di repo ini. Jika tidak ada docs-nya → nyatakan gap, jangan isi dengan asumsi.

---

## 7b. Istilah UI & URL fixture (WAJIB di TC)

Sama semangat dengan `.cursor/rules/12-jira-card-format.mdc` § Istilah UI & URL fixture (cross-repo: `olshoperp` `15-jira-card-format.mdc`).

### Label tombol / field / status

- Pakai teks **persis** dari UI aplikasi atau dari `requirement.md` / `knowledge-base.md` / capability cards.
- **Dilarang** mengarang alias (contoh salah: "Finance Complete" padahal tombol UI = **Complete**).
- `title`, `summary`, `steps`, `expected_result`, `log_summary` harus konsisten satu istilah.

### URL staging di TC

Jika TC dijalankan dengan dokumen staging yang accessible:

1. Sertakan URL **edit** di `preconditions` / `steps` / `test_data_used` / Catatan QA — bukan hanya code trx.
2. Pola: `https://staging.olshoperp.com/{module}/{menu}/edit/{id}`
3. Tabel fixture di Catatan QA wajib kolom URL yang bisa diklik.

Anti-pola: steps "klik Finance Complete" tanpa link edit Sales Return.

---

## 8. Struktur folder test-cases

```
qa-docs/{menu-slug}/
└── test-cases/
    ├── README.md          ← wajib ada, berisi daftar TC + status
    ├── TC-{PREFIX}-DRAFT-{timestamp}.md
    └── TC-{PREFIX}-NNN.md   ← setelah renumber oleh QA lead
```

`test-cases/README.md` wajib diupdate setiap kali ada TC baru — tambahkan entry ke tabel daftar TC.

---

## 9. Trigger renumber — khusus QA lead

**Trigger phrase (utama):** `#renumber-tc`

**Trigger opsional (satu menu saja):** `#renumber-tc {menu-slug}`

Contoh:
- `#renumber-tc` — scan semua menu yang punya folder `test-cases/`
- `#renumber-tc accounting-customer-payment` — hanya menu tersebut

### Scope scan

| Trigger | Folder yang di-scan |
|---------|---------------------|
| `#renumber-tc` | Semua `qa-docs/{menu-slug}/test-cases/` di repo |
| `#renumber-tc {menu-slug}` | Hanya `qa-docs/{menu-slug}/test-cases/` — verifikasi slug ada di manifest |

Menu tanpa file `TC-*-DRAFT-*.md` → dilewati (bukan error).

### Gate wajib sebelum renumber

> [!CAUTION]
> **`npm run tc:lint` harus 0 error sebelum `#renumber-tc` dijalankan.**
> Renumber memberi nomor final ke setiap DRAFT — begitu bernomor, TC duplikat
> menjadi "resmi" dan jauh lebih sulit dicabut (sudah bisa di-`recalls` flow,
> dirujuk spec, masuk laporan). Ini membuat seluruh disiplin anti-duplikat sia-sia.
>
> Khususnya, lint **menolak** TC yang punya frontmatter `duplicate_candidate: {kode}`.
> Selesaikan dulu, pilih salah satu:
> - **Hapus** file DRAFT-nya kalau memang duplikat (langkah gabungan antar TC
>   seharusnya jadi flow ber-`recalls` per rule `17`, bukan TC baru yang menyalin langkah), atau
> - **Hapus field `duplicate_candidate`** kalau setelah ditinjau ternyata unik —
>   dan jelaskan pembedanya di `summary`.

### Alur per menu (dijalankan berulang untuk tiap menu yang punya DRAFT)

1. Scan file `TC-*.md` di `qa-docs/{menu-slug}/test-cases/`
2. Pisahkan TC yang sudah bernomor (non-DRAFT) dan yang masih DRAFT (`TC-{PREFIX}-DRAFT-{timestamp}.md`)
3. Jika tidak ada DRAFT → skip menu ini
4. Sort semua DRAFT by timestamp di nama file (ascending — timestamp terkecil = nomor terkecil)
5. Tentukan nomor lanjutan dari TC bernomor terakhir yang sudah ada (jika belum ada, mulai dari 001)
6. `{PREFIX}` — dari TC bernomor existing di folder yang sama; jika folder kosong (hanya DRAFT), derive dari slug (contoh: `accounting-customer-payment` → `ARCP`)
7. Rename file DRAFT → `TC-{PREFIX}-{NNN}.md` dan update `tc_code` di frontmatter
8. Update `test-cases/README.md` menu tersebut dengan daftar TC final
9. Lanjut ke menu berikutnya yang punya DRAFT

### Laporan ke user

Setelah selesai, laporkan ringkasan per menu:
- Menu yang di-renumber: `old name → new name + tc_code`
- Menu tanpa DRAFT: sebutkan "(tidak ada DRAFT)" atau tidak perlu dilist jika banyak menu bersih

TC yang sudah bernomor (non-DRAFT) **tidak diubah nomornya** — hanya DRAFT yang di-assign nomor baru.

Trigger ini hanya dijalankan saat user mengetiknya secara eksplisit. Agent tidak boleh menjalankan renumber otomatis di situasi lain (termasuk setelah buat TC DRAFT baru).

---

## 10. README test-cases — format wajib

```markdown
# Test Cases — {Menu Name}

| TC Code | Title | Status | Automated | Last Updated |
|---------|-------|--------|-----------|-------------|
| TC-{PREFIX}-001 | {judul} | draft/review/final | ✅/❌ | YYYY-MM-DD |
```

Entry DRAFT ditulis dengan `tc_code: PENDING-{timestamp}` sampai renumber dijalankan.

---

## 8. Standar Eksekusi: Wajib Web UI Crawling & Dokumentasi Step UI Detail (WAJIB)

1. **Metode Eksekusi Default adalah Web UI Crawling:**
   - Setiap kali diminta menjalankan testcase, **HARUS SELALU** menggunakan metode **Crawling Website (Playwright Web UI Crawling / Browser Automation)**: simulasi browser langsung, pengisian form input, pemilihan dropdown, klik tombol UI, dan pengecekan datalist di browser.
   - **Metode API Testing DILARANG** digunakan sebagai pengganti eksekusi testcase kecuali jika ada permintaan eksplisit dari user di sesi chat (*as requested only*).
2. **Wajib Dokumentasi Step-by-Step UI Crawling di File TC:**
   - Pada file dokumen testcase (), bagian  dan Catatan QA **WAJIB** melengkapi langkah-langkah **Step-by-Step UI Crawling** secara mendetail (misal: navigasi route, selector/field ID yang diisi, pilihan dropdown multiselect, tombol yang diklik, hingga cara verifikasi di datalist/tabel UI).
   - Tujuannya adalah memastikan hasil testing valid, transparan, dan dapat direproduksi ulang via crawling website.
